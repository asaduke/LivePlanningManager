<%= turbo_frame_tag live do %>
  <%= form_with model: @live, local: true, class: "text-gray-600" do |f| %>
    <div class="ml-4 mr-16 text-2xl flex justify-center my-4" style="word-break: break-all;">
      <div class="text-gray-400 hover:text-gray-500 text-sm pr-2 pt-2">
        <%=link_to '編集', edit_life_path(live) %>
      </div>

      <%= @live.title %>
    </div>

    <% if @live.place.present? %>
      <div class="mx-8 xl:mx-12 mb-2" style="word-break: break-all;">
        <div class="text-teal-600">
          <%= f.label :place, t('activerecord.attributes.live.place') %><br>
        </div>

        <div class="text-xl">
          <%= @live.place %>
        </div>
      </div>
    <% end %>

    <% if @live.date.present? %>
      <div class="font-light ml-8 xl:ml-12 mb-2">
        <div class="text-teal-600">
          <%= f.label :date, t('activerecord.attributes.live.date') %><br>
        </div>

        <div class="text-xl">
          <%= @live.date.strftime('%Y年%m月%d日') %>
        </div>
      </div>
    <% end %>

    <div class="space-x-12 flex flex-row mb-2">
      <% if @live.open_time.present? %>
        <div class="font-light ml-8 xl:ml-12">
          <div class="text-teal-600">
            <%= f.label :open_time %>
          </div>

          <div class="text-xl">
            <%= @live.open_time.strftime('%H時%M分') %>
          </div>
        </div>
      <% end %>

      <% if @live.start_time.present? %>
        <div class="font-light ml-8 xl:ml-12">
          <div class="text-teal-600">
            <%= f.label :start_time %>
          </div>

          <div class="text-xl">
            <%= @live.start_time.strftime('%H時%M分') %>
          </div>
        </div>
      <% end %>
    </div>

    <% if @live.departure_time.present? %>
      <div class="font-light ml-8 xl:ml-12 mb-2">
        <div class="text-amber-600">
          <%= f.label :departure_time %>
        </div>

        <div class="text-xl">
          <%= @live.departure_time.strftime('%Y年%m月%d日%H時%M分') %>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>
